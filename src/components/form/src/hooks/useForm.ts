import type {FormTypes} from './../types'
/**
 * 使用form
 */
export const useForm = (types:FormTypes) => {

    const register = ()=>{

    }

    const methods = {

    }

    return [register,methods]
}